//3. Fazer um algoritmo que leia um número inteiro e mostre uma mensagem dizendo se é primo ou
//não.
#include <stdio.h>
int main () {
    int a;
    printf ("informe um número : ");
    scanf ("%i", &a);
    for (int i=a-1; i>=2; i-=1){
        if (a%i==0){
            printf ("não é primo !!");
            return 0;
        }
    }
    printf ("é primo !!");
            return 0;
}
