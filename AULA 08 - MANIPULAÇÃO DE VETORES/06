/*6. Faça um programa que leia um vetor de 10 elementos. Modifique o vetor de modo que o primeiro
elemento passe para a última posição, e desloque todos os outros para uma posição a esquerda.
Imprima o vetor modificado.
Exemplo:
Antes: 12 35 25 14 1 85 10 110 44 996
Depois: 35 25 14 1 85 10 110 44 996 12 */
#include <stdio.h>
int main () {
    int vet[10];
    for (int i=0; i<10; i++){
        printf ("%i ) informe um número : ", i+1);
        scanf ("%i", &vet[i]);
    }
    for (int j=0; j<9; j++){
        
        //vetvet[j-1];
        printf ("%i ", vet[j+1]);
    }
    printf ("%i", vet[0]);
}
