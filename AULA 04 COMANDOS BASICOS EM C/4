/*4. Suponha que o conceito de um aluno seja determinado em função de sua nota. Crie um algoritmo
que receba duas notas de um aluno, calcule e apresente a média e o conceito conforme a tabela
abaixo:
     Nota             Conceito
0      a     4,9     Insuficiente
5,0    a     6,4     Regular
6,5    a     8,4     Bom
8,5    a    10,0     Ótimo */
#include <stdio.h>
int main () {
    float a, b, media;
    printf ("informe suas notas :\n");
    scanf("%f", &a);
    scanf("%f", &b);
    media=(a+b)/2;
    if ((media>=0)&&(media<5)){
        printf ("sua media é %.2f e é insuficinte !!", media);
    }else if ((media>=5)&&(media<6.5)){
        printf ("sua media é %.2f e é regular !!", media);
    }else if ((media>=6.5)&&(media<8.5)){
        printf ("sua media é %.2f e é bom !!", media);
    }else if ((media>=8.5)&&(media<10.1)){
        printf ("sua media é %.2f e é ótimo !!", media);
    }else{
        printf ("erro !");
    }
}
