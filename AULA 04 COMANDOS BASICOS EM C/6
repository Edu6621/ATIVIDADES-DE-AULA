/*6. Escreva um programa que receba dois valores inteiros e mostre uma mensagem na tela
informando se os números são múltiplos ou não. */
#include <stdio.h>
int main (){
    int a, b, resultado;
    printf ("informe dois valores inteiros :\n");
    scanf("%i", &a);
    scanf("%i", &b);
    resultado=a*b;
    if((resultado%b==0) && (resultado%a==0)){
        printf ("os números são múltiplos !!");
    }else{
        printf ("os números não são múltiplos !!");
    }return 0;
}
